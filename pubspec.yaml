name: cryptomentor
description: Cryptomentor - FULL modules, Free API (CoinGecko + RSS), build-ready
publish_to: "none"
version: 1.1.0+10
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  xml: ^6.5.0
  intl: ^0.19.0
  provider: ^6.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/whales_top50.json