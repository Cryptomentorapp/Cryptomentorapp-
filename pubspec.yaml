name: cryptomentor
description: Cryptomentor - AI Trading Suite (full code + free APIs + mock fallback)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/overview.json
    - assets/data/tokens.json
    - assets/data/whales.json
    - assets/data/signals.json
    - assets/data/academy_index.json
    - assets/data/news_stub.json
