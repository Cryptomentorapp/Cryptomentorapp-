
name: cryptomentorapp
description: Cryptomentor — UI mapped to HTML + live APIs (Binance/CoinGecko/RSS)
publish_to: "none"
version: 1.0.2+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  xml: ^6.5.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/cmt_shield.png
