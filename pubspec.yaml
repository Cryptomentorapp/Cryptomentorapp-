name: cryptomentor_full
description: Cryptomentor native Flutter app (All-in-One Clean) – Scanner + Hotlist + News + Whale watchlist – no WebView
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  intl: ^0.19.0
  xml: ^6.5.0
  url_launcher: ^6.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/whales_top50.json