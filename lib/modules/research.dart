import 'package:flutter/material.dart';
class ResearchPage extends StatelessWidget { const ResearchPage({super.key});
  @override Widget build(BuildContext c)=> const Center(child: Text('Research Hub – data integration.')); }
