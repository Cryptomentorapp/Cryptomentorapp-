import 'package:flutter/material.dart'; class PricingPage extends StatelessWidget{ const PricingPage({super.key}); @override Widget build(BuildContext c)=>ListView(children: const [ ListTile(title: Text('Free/Pro')), ]);}