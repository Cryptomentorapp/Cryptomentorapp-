import 'package:flutter/material.dart'; class CMTHubPage extends StatefulWidget{ const CMTHubPage({super.key}); @override State<CMTHubPage> createState()=>_C(); } class _C extends State<CMTHubPage>{ bool listed=false; double mockPrice=0.0123; @override Widget build(BuildContext c)=>ListView(padding: const EdgeInsets.all(16), children:[ SwitchListTile(title: const Text('CMT Listed on exchange?'), value: listed, onChanged:(v)=>setState(()=>listed=v)), const SizedBox(height:8), if(!listed) const Text('CMT chưa list — hiển thị giá nội bộ (mock).'), Text('Giá CMT hiện tại: '+(listed? 'Đang lấy từ sàn...' : '\$'+mockPrice.toStringAsFixed(4))), ]); }