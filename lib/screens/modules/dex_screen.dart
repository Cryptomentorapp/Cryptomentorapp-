import 'package:flutter/material.dart';
class DEXoutsidestoreScreen extends StatelessWidget {
  const DEXoutsidestoreScreen({super.key});
  @override Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('DEX (outside store)')), body: const Center(child: Text('DEX (outside store) — Coming soon')));
  }
}
