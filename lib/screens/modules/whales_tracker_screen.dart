import 'package:flutter/material.dart';
class WhalesTrackerScreen extends StatelessWidget {
  const WhalesTrackerScreen({super.key});
  @override Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Whales Tracker')), body: const Center(child: Text('Whales Tracker — Coming soon')));
  }
}
