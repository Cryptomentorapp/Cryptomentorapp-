import 'package:flutter/material.dart';
class LaunchpadoutsidestoreScreen extends StatelessWidget {
  const LaunchpadoutsidestoreScreen({super.key});
  @override Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Launchpad (outside store)')), body: const Center(child: Text('Launchpad (outside store) — Coming soon')));
  }
}
